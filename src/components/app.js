import React, { Component } from 'react';
import StopwatchContainer from './stopwatch_container';

export default class App extends Component {
  render() {
    return (
      <div>
        <StopwatchContainer />
      </div>
    );
  }
}
